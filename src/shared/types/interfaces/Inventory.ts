export interface InventoryInput {
    inventory_id: number,
    film_id: number,
    store_id: number,
}

export interface InventoryOutput {
    inventory_id: number,
    film_id: number,
    store_id: number,
}